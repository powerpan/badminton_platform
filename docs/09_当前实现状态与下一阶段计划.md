# 当前实现状态与下一阶段计划

## 1. 当前代码状态

截至当前阶段，项目已经完成从任务书到可运行业务版本的第三轮落地。当前版本包含用户认证闭环、场地预约闭环、后台基础管理、规则配置、统计分析、操作日志和预约状态自动处理。

已完成内容：

- 后端 Tornado 应用入口、路由和统一 JSON 响应。
- MySQL 连接池、基础查询封装和核心 repository。
- Redis、MySQL、API 健康检查。
- 用户注册、登录、JWT 鉴权。
- 当前用户信息查询、资料修改和密码修改。
- 场地列表和后台场地维护。
- 按配置生成场地时间段，支持 `available/reserved/locked/disabled` 状态。
- 预约创建，使用 Redis `SET NX EX` 做并发窗口锁。
- MySQL 时间重叠冲突校验，最终防止重复预约。
- 我的预约列表和未开始预约取消。
- 管理员用户新增、启用/禁用、角色修改。
- 管理员预约列表、详情和取消预约。
- 管理员公告发布、编辑、显示/隐藏。
- 管理员规则配置查询和更新。
- 后台统计总览、场地使用率、热门时间段和用户活跃度接口。
- 后台统计视图，支持按日期范围查看统计数据。
- 管理员关键写操作日志，包括用户、场地、预约、公告和规则配置。
- 操作日志查询视图，支持按模块、操作人、日期筛选。
- 预约状态自动处理，查询预约和统计前会将已结束 `confirmed` 预约更新为 `completed`。
- Vue3 前端基础布局、路由、Pinia 登录态和 Axios 拦截器。
- 登录页、注册页、个人中心页、场地预约页、我的预约页、后台管理页。
- 前端路由守卫：未登录不能访问业务页，普通用户不能访问后台。
- 初始化 SQL、默认场地、默认配置和默认管理员账号。

默认管理员账号：

```text
username: admin
password: admin123456
```

说明：默认管理员只用于本地开发和演示，正式环境需要修改密码和 JWT 密钥。

## 2. 当前可用接口

### 2.1 健康检查

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/health` | 检查 API、MySQL、Redis 状态 | 无需登录 |
| GET | `/api/admin/health` | 管理员健康检查 | 管理员 |

### 2.2 认证接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| POST | `/api/auth/register` | 注册普通用户 | 无需登录 |
| POST | `/api/auth/login` | 登录并返回 JWT | 无需登录 |
| GET | `/api/auth/profile` | 获取当前用户资料 | 登录用户 |
| PUT | `/api/auth/profile` | 修改昵称和联系方式 | 登录用户 |
| PUT | `/api/auth/password` | 修改密码 | 登录用户 |

### 2.3 用户端业务接口

| 方法 | 路径 | 说明 | 权限 |
| --- | --- | --- | --- |
| GET | `/api/announcements` | 查询显示中的公告 | 无需登录 |
| GET | `/api/announcements/{id}` | 查询公告详情 | 无需登录 |
| GET | `/api/courts` | 查询启用场地 | 登录用户 |
| GET | `/api/courts/{court_id}/slots?date=YYYY-MM-DD` | 查询场地时间段 | 登录用户 |
| POST | `/api/reservations` | 创建预约 | 登录用户 |
| GET | `/api/reservations/my` | 查询我的预约 | 登录用户 |
| PUT | `/api/reservations/{id}/cancel` | 取消我的预约 | 登录用户 |

### 2.4 管理员接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/api/admin/users` | 用户列表和新增用户 |
| PUT | `/api/admin/users/{id}/status` | 启用或禁用用户 |
| PUT | `/api/admin/users/{id}/role` | 修改用户角色 |
| GET/POST | `/api/admin/courts` | 场地列表和新增场地 |
| PUT | `/api/admin/courts/{id}` | 编辑场地 |
| PUT | `/api/admin/courts/{id}/status` | 启用或停用场地 |
| GET | `/api/admin/reservations` | 查询全部预约 |
| GET | `/api/admin/reservations/{id}` | 查询预约详情 |
| PUT | `/api/admin/reservations/{id}/cancel` | 管理员取消预约 |
| GET/POST | `/api/admin/announcements` | 公告列表和发布公告 |
| PUT | `/api/admin/announcements/{id}` | 编辑公告 |
| PUT | `/api/admin/announcements/{id}/status` | 显示或隐藏公告 |
| GET | `/api/admin/configs` | 查询规则配置 |
| PUT | `/api/admin/configs/{config_key}` | 更新规则配置 |

## 3. 当前前端页面

| 路径 | 页面 | 状态 |
| --- | --- | --- |
| `/` | 首页、服务状态、场馆公告 | 已接入健康检查和公告列表 |
| `/login` | 登录页 | 已接入后端登录接口 |
| `/register` | 注册页 | 已接入后端注册接口 |
| `/profile` | 个人中心 | 已接入资料和密码接口 |
| `/courts` | 场地预约 | 已接入场地、日期、时间段和预约提交 |
| `/reservations` | 我的预约 | 已接入预约列表、状态筛选和取消 |
| `/admin` | 管理后台 | 已接入统计、用户、场地、预约、公告、规则、日志七个视图 |

## 4. 已验证内容

已完成的验证包括：

- `npm run build` 构建通过。
- 后端 Python 编译通过。
- `git diff --check` 通过。
- `/api/health` 返回 API、MySQL、Redis 均为 `ok`。
- 管理员账号可以登录。
- 普通用户可以注册、登录。
- 普通用户可以查询场地列表和时间段状态。
- 普通用户创建预约成功。
- 重复预约同一场地同一日期同一时间段返回 `409`。
- 我的预约能看到刚创建的预约。
- 用户可以取消自己的未开始预约。
- 普通用户访问 `/api/admin/users` 返回 `403`。
- 管理员可以新增用户、修改角色、禁用用户。
- 管理员可以新增、编辑、停用场地。
- 管理员可以查询预约列表。
- 管理员可以发布、编辑、隐藏公告。
- 管理员可以查询和更新规则配置。
- 管理员可以查询统计总览、场地使用率、热门时间段和用户活跃度。
- 管理员关键操作会写入 `operation_log`。
- 管理员可以在后台查询操作日志。
- 已结束的 `confirmed` 预约会自动转为 `completed`。
- 浏览器抽查通过：普通用户后台路由被拦截，管理员可进入后台，场地预约页可提交预约并刷新状态。

## 5. 下一阶段建议推送内容

下一阶段建议推送“测试与部署稳定化”。当前核心业务、统计和日志已经具备，下一阶段应提升自动化回归、环境稳定性和验收可重复性。按当前要求，本阶段不做一键演示脚本。

建议包含以下内容：

1. 后端自动化接口测试：覆盖认证、预约、统计、日志、后台权限和规则配置。
2. 测试数据准备和清理流程：保证接口测试可重复运行。
3. Docker Compose 或生产部署配置：统一 MySQL、Redis、后端、前端环境。
4. 安全增强：刷新 token、验证码、找回密码、默认管理员改密提醒。
5. 页面细节完善：补充空状态、错误提示、分页和筛选体验。
6. 验收材料整理：准备接口测试结果、页面截图和数据库说明。

完成后应达到：

- 核心接口有自动化回归保障。
- 测试数据可以自动准备和清理。
- 部署环境可以稳定复现。
- 默认账号和 token 相关安全风险有基础处理。
- 验收材料能直接说明系统功能、接口和数据状态。

## 6. 下一阶段暂不建议优先做的内容

以下内容可以继续后置：

- 支付。
- 短信验证码。
- 会员等级。
- 积分。
- 复杂排班。
- 小程序端。

原因是这些扩展会增加业务复杂度。当前项目更需要先把统计、日志、测试和部署补齐，这些内容更直接支撑课程验收和演示。

## 7. 下一阶段验收标准

下一阶段完成后，至少需要通过：

- 后端接口测试可以覆盖核心流程并重复运行。
- 测试数据准备和清理不会污染演示数据。
- Docker Compose 或部署配置可启动依赖服务和应用。
- 安全增强不破坏现有登录和路由守卫。
- `npm run build` 通过。
- 后端 Python 编译通过。
- `GET /api/health` 正常。
- `git diff --check` 无格式问题。
