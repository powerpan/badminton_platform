# 内容填充与赛事配图

2026-09-19，按用户要求补充当前项目空置的内容板块，并为赛事增加写实场景配图。

## 已导入内容

导入前：活动 0、公告 0、球友圈 0；商城已有 3 件商品。

| 板块 | 数量 | 内容 |
|---|---:|---|
| 活动赛事 | 6 | 新手基础训练、周末双打交流赛、混双搭档交流、下班后双打、单打循环赛、发接发专项 |
| 公告 | 4 | 预约入场、活动报名取消、会员余额权益、球友圈与场地礼仪 |
| 球友圈 | 6 | 入门练习、双打配合、训练打卡、手胶器材、活动讨论、场边提醒 |

本次活动日期为 2026-09-26 至 2026-10-04，截止日期为活动前一天 22:00；人数上限 12 或 16。活动当前可以报名，初始报名人数均为 0，没有创建虚构参与者或报名记录。

所有条目标注“演示”，球友圈以管理员发布的交流话题呈现，不冒充其他用户的真实经历。活动详情说明不代表真实举办安排、报名不扣余额且不会自动占用预约场地。没有新增用户、余额、订单、预约、维护时段或群发通知。

## 可重复导入

```bash
# 只读预览当前本地库，不写入
backend/.venv/bin/python scripts/seed_content.py
# 插入缺失的示例条目，整批事务提交
backend/.venv/bin/python scripts/seed_content.py --apply
```

仅允许 `.env` 指定的本机开发环境。默认以执行日确定未来周六和后续活动时间，可用 `--date YYYY-MM-DD` 设置基准日。既有同名演示活动/公告与相同正文动态直接跳过，不覆盖修改过的内容，不重新发布被隐藏的条目，也不会自动延长已有活动时间。活动过期后可以在后台调整日期。

本次首次执行插入 16 条，第二次执行插入 0 条、跳过 16 条；导入批次写入操作日志。新增赛事 ID 1–6、公告 ID 1–4、动态 ID 1–6；后台可分别编辑或隐藏。`create_demo.py` 仍只用于新建独立完整演示库，本脚本只补当前库内容。

## 配图

采用内置 Image Gen，场景为双打、基础训练、单打、混双交流、晚间双打、发球练习。只匹配这六个明确标注演示的活动标题；不把生成图片自动分配给其他真实活动。赛事列表和详情共用 `EventImage.vue`，图片显示“AI 生成 · 场景示意”，加载失败则隐藏图片，活动正文和报名功能仍可用。修改演示标题后，可在该组件中同步调整对应关系。

图片提供 1280px 详情版本和 640px 卡片版本，以 `srcset` 随显示宽度选择；列表采用懒加载。没有替换现有首页或商品图片。

填充内容后顺带修复两个实际显示问题：球友圈三行截断却无展开入口，改为完整展示正文；手机活动详情受到旧全局 `table { min-width: 620px }` 影响，地点及报名按钮被裁切，使用该页面专属的表格宽度与换行规则修复。活动列表调整为桌面三列、平板两列、手机单列。

资源位于 `frontend/public/events/`：`training.jpg`、`doubles.jpg`、`mixed.jpg`、`evening.jpg`、`singles.jpg`、`serve.jpg`，每张另有 `-640.jpg` 缩略版本。完整生成提示词见 [赛事配图生成提示词](design/event-image-prompts.md)。原始生成结果保留在工具默认目录，项目引用的是仓库内 JPEG 文件，不依赖个人临时路径。

## 验证

- 本地 API/MySQL/Redis 均正常；活动、公告、动态接口分别返回 6、4、6 条，商城仍为 3 件商品。
- 活动日期及正文通过既有业务校验；六场活动均可报名，报名人数为 0。重复导入没有新增行或重复操作日志。
- 用户仍为 5 个、预约仍为 6 条，活动报名仍为 0；脚本没有修改会员账户或资金流水。
- 浏览器实看首页 3 条公告入口、4 条公告列表、6 条完整球友话题、6 个赛事卡片与详情配图；六张图片均加载成功。
- 检查 1440×1000、768×1024、390×844，赛事列表分别为三/二/一列，无整页横向溢出；手机详情的报名按钮位于屏幕内，访客点击后跳转到带活动返回地址的登录页，没有实际创建报名。
- 前端类型检查和生产构建通过，脚本语法检查与 `git diff --check` 通过。原有主包体积/PURE 注释提示保留。
- 本次没有修改表结构、推送 Git 或部署到外部服务器；前端页面通过当前本地业务服务读取新增内容。
